# Frontend

React + Fluent UI dashboard.

Screens: repository intake, readiness dashboard, findings, and recommendations.
Backend support for these views lives in `backend/Validation/Dashboard/`.
